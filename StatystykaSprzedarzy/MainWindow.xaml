<Window x:Class="StatystykaSorzedarzy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StatystykaSorzedarzy"
        mc:Ignorable="d"
        Title="Statystyka Sprzedaży" Height="550" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button x:Name="btnWybierz" Content="Wybierz plik..." 
                    Click="btnWybierz_Click" 
                    Height="30" Width="120" Margin="0,0,10,0" Background="#FF3E3E3E" Foreground="White"/>
            <Button x:Name="btnDodaj" Content="Dodaj produkt..." 
                    Click="btnDodaj_Click"
                    Height="30" Width="120" Margin="0,0,10,0" Foreground="Black" Background="#FF1AD600"/>
            <Button Content="Pokaż statystyki..." 
                    Click="Button_Click"
                    Height="30" Width="120" Background="#FFCE1212" Foreground="White"/>
            <Button Content="Zapisz zmiany do pliku" 
                    Click="btnZapiszDoPliku_Click"
                    Height="30" Width="150" Margin="100,0,0,0" Foreground="White" Background="#FF3E3E3E"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Margin="10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtNowyProdukt" Grid.Column="0" 
                         Margin="0,0,5,0" Height="25" 
                         ToolTip="Nazwa produktu"/>
                <TextBox x:Name="txtNowaIlosc" Grid.Column="1" 
                         Margin="5,0,5,0" Height="25"
                         ToolTip="Ilość"/>
                <TextBox x:Name="txtNowaCena" Grid.Column="2" 
                         Margin="5,0,5,0" Height="25"
                         ToolTip="Cena"/>
               
            </Grid>
        </GroupBox>

        <ListView Grid.Row="2" Name="listViewMain" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Produkt" Width="250" DisplayMemberBinding="{Binding Produkt}"/>
                    <GridViewColumn Header="Ilość" Width="150" DisplayMemberBinding="{Binding Ilosc}"/>
                    <GridViewColumn Header="Cena" Width="150" DisplayMemberBinding="{Binding Cena, StringFormat='{}{0:F2} zł'}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
            <Label Name="ilcPoz" Content="Ilość pozycji: 0" 
                   VerticalAlignment="Center" Height="30" Width="160"/>
            <Label Name="ilcPro" Content="Ilość produktów: 0" 
                   VerticalAlignment="Center" Height="30" Width="160"/>
        </StackPanel>
    </Grid>
</Window>
